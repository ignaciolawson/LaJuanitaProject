package com.lajuanita.backend.alumno;

/** Nivel con el que la persona entró al estudio. Coincide con el CHECK {@code alumno_nivel_ingreso_valido}. */
public enum NivelIngreso {
    INICIAL,
    INTERMEDIO,
    AVANZADO
}
